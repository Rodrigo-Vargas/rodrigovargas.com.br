import CoverLetterTemplate, { CoverLetterInfoType } from "src/templates/CoverLetter";

const data: CoverLetterInfoType = {
    
}

export default function Resume() {
    return <CoverLetterTemplate data={data} />;
  }
  